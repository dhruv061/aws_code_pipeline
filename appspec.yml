version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/
hooks:
  BeforeInstall:
    - location: scripts/befor_server
      timeout: 300
      runas: ubuntu
  AfterInstall:
    - location: scripts/restart_server
      timeout: 300
      runas: ubuntu
